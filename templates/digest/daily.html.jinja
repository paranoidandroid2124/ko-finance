<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <title>K-Finance Digest</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        background-color: #f6f7fb;
        font-family: "Pretendard", "Apple SD Gothic Neo", "Noto Sans KR", Arial, sans-serif;
        color: #101217;
      }
      .container {
        max-width: 720px;
        margin: 32px auto;
        background-color: #ffffff;
        border-radius: 20px;
        box-shadow: 0 12px 30px rgba(16, 18, 23, 0.08);
        overflow: hidden;
      }
      .header {
        background: linear-gradient(120deg, #1f6feb, #6c5dd3);
        padding: 36px 40px 28px;
        color: #ffffff;
      }
      .header h1 {
        margin: 0;
        font-size: 26px;
      }
      .header p {
        margin: 10px 0 0;
        font-size: 14px;
        opacity: 0.9;
      }
      .body {
        padding: 32px 40px 40px;
      }
      .section-title {
        font-size: 14px;
        font-weight: 600;
        letter-spacing: 0.08em;
        color: #6c5dd3;
        text-transform: uppercase;
        margin-bottom: 10px;
      }
      .card {
        border: 1px solid #e4e8f5;
        border-radius: 16px;
        padding: 20px;
        margin-bottom: 24px;
      }
      .news-item,
      .watchlist-item,
      .action-item {
        border-bottom: 1px solid #eceff7;
        padding: 14px 0;
      }
      .news-item:last-child,
      .watchlist-item:last-child,
      .action-item:last-child {
        border-bottom: none;
      }
      .news-item h3,
      .watchlist-item h3,
      .action-item h3 {
        margin: 0 0 6px;
        font-size: 16px;
      }
      .meta {
        font-size: 13px;
        color: #5a6072;
      }
      .badge {
        display: inline-flex;
        align-items: center;
        padding: 2px 8px;
        font-size: 11px;
        font-weight: 600;
        border-radius: 999px;
        background-color: #f0f2fb;
        color: #5a6072;
        margin-right: 6px;
      }
      .sentiment-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
        gap: 12px;
      }
      .sentiment-pill {
        padding: 10px;
        border-radius: 12px;
        background-color: #f5f7fb;
        font-size: 13px;
      }
      .footer {
        text-align: center;
        font-size: 12px;
        color: #97a0b5;
        padding: 20px 10px 28px;
      }
      a {
        color: #1f6feb;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>K-Finance Digest · {{ "Daily" if timeframe == "daily" else "Weekly" }}</h1>
        <p>{{ period_label }} · {{ generated_label }}</p>
        {% if source_label %}
        <p style="margin-top: 4px; font-size: 12px; opacity: 0.8;">데이터 출처: {{ source_label }}</p>
        {% endif %}
      </div>
      <div class="body">
        {% if llm_overview %}
        <div class="card">
          <div class="section-title">LLM Insight</div>
          <div style="font-size: 14px; line-height: 1.6;">
            {% for line in llm_overview.split('\n') %}
            <p style="margin: 0 0 8px;">{{ line }}</p>
            {% endfor %}
          </div>
        </div>
        {% endif %}

        {% if sentiment %}
        <div class="card">
          <div class="section-title">Sentiment Pulse</div>
          <p style="margin: 0 0 16px; font-size: 14px;">{{ sentiment.summary }}</p>
          <div class="sentiment-grid">
            {% for indicator in sentiment.indicators %}
            <div class="sentiment-pill">
              <div style="font-size: 12px; color: #5a6072;">{{ indicator.name }}</div>
              <div style="font-size: 16px; font-weight: 600; margin-top: 4px;">{{ indicator.value }}</div>
            </div>
            {% endfor %}
          </div>
        </div>
        {% endif %}

        <div class="card">
          <div class="section-title">뉴스 하이라이트</div>
          {% if news %}
          {% for item in news %}
          <div class="news-item">
            <h3>{{ item.headline }}</h3>
            {% if item.summary %}<p style="margin: 0 0 6px; font-size: 14px; line-height: 1.5;">{{ item.summary }}</p>{% endif %}
            <div class="meta">
              {% if item.source %}
              <span class="badge">{{ item.source }}</span>
              {% endif %}
              {% if item.link %}
              <a href="{{ item.link }}">원문 보기 →</a>
              {% endif %}
            </div>
          </div>
          {% endfor %}
          {% else %}
          <p style="margin: 0; font-size: 14px; color: #5a6072;">표시할 뉴스가 없습니다.</p>
          {% endif %}
        </div>

        <div class="card">
          <div class="section-title">워치리스트 업데이트</div>
          {% if watchlist %}
          {% for item in watchlist %}
          <div class="watchlist-item">
            <h3>{{ item.title }}</h3>
            <p style="margin: 0 0 4px; font-size: 14px;">{{ item.description }}</p>
            <div class="meta">
              {% if item.changeLabel %}
              <span class="badge">{{ item.changeLabel }}</span>
              {% endif %}
              <span class="badge">{{ item.tone|default("neutral")|capitalize }}</span>
            </div>
          </div>
          {% endfor %}
          {% else %}
          <p style="margin: 0; font-size: 14px; color: #5a6072;">최근 워치리스트 알림이 없습니다.</p>
          {% endif %}
        </div>

        {% if actions %}
        <div class="card">
          <div class="section-title">오늘의 체크리스트</div>
          {% for action in actions %}
          <div class="action-item">
            <h3>{{ action.title }}</h3>
            <p style="margin: 0; font-size: 14px;">{{ action.note }}</p>
          </div>
          {% endfor %}
        </div>
        {% endif %}

        {% if llm_personal_note %}
        <div class="card">
          <div class="section-title">Personal Note</div>
          <div style="font-size: 14px; line-height: 1.6;">
            {% for line in llm_personal_note.split('\n') %}
            <p style="margin: 0 0 8px;">{{ line }}</p>
            {% endfor %}
          </div>
        </div>
        {% endif %}
      </div>
      <div class="footer">
        K-Finance Evidence Copilot · Digest {{ timeframe|capitalize }}
      </div>
    </div>
  </body>
</html>
