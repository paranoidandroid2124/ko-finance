import { describe, it } from "vitest";

describe.skip("alerts plan gating (playwright placeholder)", () => {
  it("free tier surfaces plan lock messaging", () => {
    // Playwright end-to-end coverage lives in the dashboard integration suite.
    // This test is skipped under Vitest to avoid bundler resolution errors.
  });
});
